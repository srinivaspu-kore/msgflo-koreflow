exports.parseName = function(name) {
  var nameParts, parsed;
  if (name.indexOf('/') === -1) {
    return parsed = {
      library: null,
      name: name
    };
  }
  nameParts = name.split('/');
  return parsed = {
    library: nameParts[0],
    name: nameParts[1]
  };
};
